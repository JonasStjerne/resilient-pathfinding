{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsc -w & browser-sync --server 'index.html' --files './dist/**/*.js' --no-open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  }
}
