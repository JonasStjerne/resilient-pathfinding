{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsc -w & browser-sync --server 'index.html' --files './dist/**/*.js'",
    "ts:watch": "tsc -w",
    "bs:watch": "browser-sync --server 'index.html' --files './dist/**/*.js'--no-open",
    "start:windows": "concurrently \"npm run ts:watch\" \"npm run bs:watch\"",
    "dev:debug": "tsc -w & browser-sync --server 'index.html' --files './dist/**/*.js' --no-open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
