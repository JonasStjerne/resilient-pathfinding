import { board } from "../board";

class aStar{
    constructor(heuristic: string = "manhattan"){
        
    }

    findPath(board: board, startX: number, startY: number, endX: number, endY: number): number[][]{
        return [];
    }
}